# Copy to .env and fill in your real key. .env is gitignored.
# NOTE: confirm the exact variable name the TypeSafe SDK reads in their docs —
# this is the conventional name, not one verified against the live docs.
TYPESAFE_API_KEY=
